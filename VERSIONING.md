# วิธีออกเวอร์ชัน (Versioning Guide) — Pizza Damac

คู่มือสั้น ๆ สำหรับทุกครั้งที่แก้โค้ดแล้วอัปขึ้น GitHub
เป้าหมาย: ย้อนดูได้ว่า **เวอร์ชันไหน แก้อะไร เมื่อไหร่ ทำไม** และรู้ว่าเครื่องที่ร้านรันเวอร์ชันไหนอยู่

---

## 1. เลขเวอร์ชันหมายถึงอะไร — `MAJOR.MINOR.PATCH` เช่น `1.1.0`

| ตำแหน่ง | ขยับเมื่อ | ตัวอย่าง |
|---|---|---|
| **PATCH** (ตัวท้าย) | แก้บั๊กเล็ก ๆ ไม่มีฟีเจอร์ใหม่ พฤติกรรมเดิมเหมือนเดิม | `1.1.0` → `1.1.1` แก้ข้อความผิด, แก้ปุ่มไม่ทำงาน |
| **MINOR** (ตัวกลาง) | เพิ่มฟีเจอร์ใหม่ หรือแก้บั๊กที่เปลี่ยนวิธีทำงานพอสมควร แต่ของเดิมยังใช้ได้ | `1.1.1` → `1.2.0` เพิ่มระบบสมาชิก, แก้บั๊กเรียกไรเดอร์ซ้ำ |
| **MAJOR** (ตัวหน้า) | เปลี่ยนใหญ่จนของเดิมใช้แบบเก่าไม่ได้ / ต้องย้ายข้อมูล / เปลี่ยนโครงสร้างฐานข้อมูลครั้งใหญ่ | `1.2.0` → `2.0.0` เปลี่ยนระบบล็อกอินทั้งหมด, ย้ายฐานข้อมูล |

กติกาง่าย ๆ: ขยับตัวไหน ตัวที่อยู่ขวามันกลับเป็น 0

---

## 2. ขั้นตอนออกเวอร์ชันใหม่ (ทำทุกครั้งที่ deploy)

### ขั้น 1 — จดการเปลี่ยนแปลงใน `CHANGELOG.md`
เปิด `CHANGELOG.md` แล้วเพิ่มหัวข้อใหม่ **ใต้ `## [Unreleased]`** ตามแบบนี้:

```markdown
## [1.2.0] — 2026-10-01

### Added
- <ฟีเจอร์ใหม่ที่เพิ่ม — เขียนว่าผู้ใช้จะเห็นอะไรต่างไป>

### Fixed
- **<ชื่อบั๊กสั้น ๆ>** — commit `abc1234`
  - **อาการ:** <ผู้ใช้เจออะไร>
  - **สาเหตุ:** <ต้นตอในโค้ด/ฐานข้อมูล>
  - **แก้:** <เปลี่ยนไฟล์ไหน อย่างไร>
```

หมวดที่ใช้ได้: `Added` `Changed` `Fixed` `Security` `Removed`
เขียนเป็นภาษาคน ไม่ต้องเป็นภาษาโปรแกรมเมอร์ — คนอ่านคือตัวเราเองในอีก 6 เดือน

### ขั้น 2 — เปลี่ยนเลขเวอร์ชันใน `package.json`
บรรทัด `"version": "1.4.0"` → เปลี่ยนเป็นเลขใหม่ เช่น `"version": "1.5.0"`
(เลขนี้จะโชว์ที่ `https://pizzadamac.com/api/version` หลัง deploy)

### ขั้น 3 — อัปโหลดไฟล์ขึ้น GitHub (repo `oat299-sudo/pizza-damac-delivery-v2`, branch `main`)
ข้อความ commit ให้ใช้รูปแบบนี้เพื่อให้ค้นหาย้อนหลังง่าย:

| ประเภท | ตัวอย่างข้อความ commit |
|---|---|
| แก้บั๊ก | `fix: เรียกไรเดอร์ซ้ำ — บันทึก lalamove_order_id ลง DB` |
| ฟีเจอร์ใหม่ | `feat: Promo Board ตั้งค่าโปรจากหน้า POS` |
| เปลี่ยน/ปรับ | `change: ค่าส่งใช้ราคา Lalamove จริง` |
| ออกเวอร์ชัน | `chore: release v1.2.0` |

Cloud Build จะ build + deploy ขึ้น Cloud Run อัตโนมัติ (~2–3 นาที) ดูผลได้ที่
Google Cloud Console → Cloud Build → History (โปรเจกต์ `pizza-damac-delivery`)

### ขั้น 4 — สร้าง Release บน GitHub (ปักหมุดเวอร์ชัน)
1. ไปที่ https://github.com/oat299-sudo/pizza-damac-delivery-v2/releases/new
2. **Choose a tag** → พิมพ์ `v1.2.0` (ตัว v นำหน้า) → เลือก "Create new tag"
3. **Release title** → `v1.2.0 — <สรุปสั้น ๆ 1 ประโยค>`
4. **Describe** → คัดลอกหัวข้อของเวอร์ชันนั้นจาก `CHANGELOG.md` มาวาง
5. กด **Publish release**

ผลลัพธ์: หน้า https://github.com/oat299-sudo/pizza-damac-delivery-v2/releases จะเป็นไทม์ไลน์ทุกเวอร์ชัน กดดูโค้ด ณ เวอร์ชันนั้นได้ทันที และย้อนกลับ (rollback) ไปเวอร์ชันเก่าได้ถ้าจำเป็น

### ขั้น 5 — ตรวจว่าเวอร์ชันใหม่ขึ้นจริง
เปิด `https://pizzadamac.com/api/version` ต้องเห็น `"version": "1.2.0"`
แล้วรีเฟรชแท็บ POS/Kitchen ที่ร้าน 1 ครั้ง (หรือรอระบบเด้งเตือนให้รีเฟรช)

---

## 3. ถ้าแก้หลายอย่างก่อนจะ deploy
จดแต่ละอย่างไว้ใต้ `## [Unreleased]` ไปเรื่อย ๆ ก่อน
พอจะ deploy จริงค่อยเปลี่ยนหัวข้อ `[Unreleased]` เป็นเลขเวอร์ชัน + วันที่ แล้วทำขั้น 2–5

## 4. ถ้าต้องย้อนกลับเวอร์ชันเก่า (rollback)
ทางเร็วสุด: Google Cloud Console → Cloud Run → `pizza-damac-delivery-v2` → **Revisions** → เลือก revision ก่อนหน้า → **Manage traffic** → ส่ง 100% ไป revision นั้น (ไม่ต้องแก้โค้ด มีผลใน 1 นาที)
แล้วค่อยกลับมาแก้โค้ดใน GitHub ให้ถูกและออกเวอร์ชัน PATCH ใหม่

---

## ประวัติเวอร์ชันย่อ
| เวอร์ชัน | วันที่ | สรุป |
|---|---|---|
| 1.0.0 | 2026-07-13 | ระบบฐาน: รวมเป็น 1 แอปบน Cloud Run, ล็อกความปลอดภัยครบ, Lalamove จริง, สมาชิก/โปร/สต๊อก/ต้นทุน, LINE OA, PWA |
| 1.1.0 | 2026-09-13 | แก้บั๊กเรียกไรเดอร์ Lalamove ซ้ำ + เริ่มระบบจดเวอร์ชัน |
| 1.2.0 | 2026-09-13 | ป้ายเวอร์ชันบนเว็บ (หน้าลูกค้า / หน้าล็อกอิน / POS) |
| 1.2.1 | 2026-09-13 | ย้ายป้ายเวอร์ชันใน POS ไปใต้โลโก้ (มองเห็นเสมอ) |
| 1.3.0 | 2026-09-13 | หน้า CRM ใหม่: กลุ่มลูกค้า, แผงรายละเอียด, โน้ต/แท็กพนักงาน |
| 1.4.0 | 2026-09-13 | โหมดแพลตฟอร์ม (Grab/LINE MAN) กดครั้งเดียว + ตั้งค่า % GP เองได้ + รายงานแนวโน้มรายเดือน |

รายละเอียดเต็มอยู่ใน [CHANGELOG.md](./CHANGELOG.md)
